% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LMS.R, R/Opal.R
\name{createCourseGroup}
\alias{createCourseGroup}
\alias{createCourseGroup,missing-method}
\alias{createCourseGroup,Opal-method}
\title{Create a group in a course}
\usage{
createCourseGroup(object, course_id, name, ...)

\S4method{createCourseGroup}{missing}(object, course_id, name, ...)

\S4method{createCourseGroup}{Opal}(
  object,
  course_id,
  name,
  description = "",
  minParticipants = 0,
  maxParticipants = 0,
  invitationEnabled = TRUE,
  signoutEnabled = TRUE
)
}
\arguments{
\item{object}{An S4 object of class \link{Opal} that represents a connection to
the LMS.}

\item{course_id}{A character vector of length one specifying the course
resource ID. Note that this is not the course ID shown in the URL, but a
longer identifier available via the "Show more information" option within
the course.}

\item{name}{A character vector of length one with the group name.}

\item{...}{Additional arguments to be passed to the method, if applicable.}

\item{description}{A character vector of length one with the group
description. Defaults to an empty string.}

\item{minParticipants}{A numeric value with the minimum number of
participants. Defaults to \code{0}.}

\item{maxParticipants}{A numeric value with the maximum number of
participants. Defaults to \code{0}.}

\item{invitationEnabled}{A boolean value. If \code{TRUE}, invitations are enabled
for new group members. Defaults to \code{TRUE}.}

\item{signoutEnabled}{A boolean value. If \code{TRUE}, members may leave the
group themselves. Defaults to \code{TRUE}.}
}
\value{
A data frame with the created group attributes.

A one-row data frame with the created group attributes, or \code{NULL}
when OPAL rejects the request.
}
\description{
This method creates a group in a course on the Learning Management System
(LMS). If no LMS connection object is provided, it attempts to guess the
connection using default settings (e.g., environment variables). If the
connection cannot be established, an error is thrown.

This method creates a learning group in an LMS Opal course using OPAL's
\code{GroupVO} XML representation. The group name must be unique within the
course. \code{invitationEnabled} controls whether new group members receive an
invitation that has to be accepted; \code{signoutEnabled} controls whether members
may leave the group themselves.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
group <- createCourseGroup("89068111333293", "Topic 5")
\dontshow{\}) # examplesIf}
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
group <- createCourseGroup(
    "89068111333293",
    "Topic 5",
    description = "Group for topic 5",
    minParticipants = 1,
    maxParticipants = 1,
    invitationEnabled = FALSE,
    signoutEnabled = FALSE
)
\dontshow{\}) # examplesIf}
}
